import React from "react";

interface UserPageI {
	// userId: number,
}

const UserPage: React.FC<UserPageI> = ({}: UserPageI) => {

    return (
        <div>
        	user
        </div>
    );
};

export default UserPage;
